&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
    Если ПараметрыЗаписи.Ключ.Пустая() Тогда
        Отказ = НЕ (Справочники.Пользователи.ПустаяСсылка() = Справочники.Пользователи.НайтиПоНаименованию(СокрЛП(ТекущийОбъект.Наименование), Истина));
        Сообщить("Пользователь с указанным логином уже существует");
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура АктивностьПриИзменении(Элемент)
    Объект.КоличествоПопыток = 0;
КонецПроцедуры